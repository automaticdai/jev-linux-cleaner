# run artifacts contain absolute paths and filenames from the scanned machine
runs/
*.run.json
plan.sh

# python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# local config overrides
policy.local.yaml
roots.local.yaml
.env
.venv/

# a captured scan names real directories (and so real projects) on the machine
# that produced it; regenerate locally with scripts/capture_scan.py
tests/fixtures/scan-ubuntu-2404.json
